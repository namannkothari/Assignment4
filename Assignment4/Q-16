#include<conio.h>
#include<stdio.h>

void main()
{
    
	char a;
	printf("enter the alphabet: ");
	scanf("%c",&a);
	if(a == 'a' || a == 'e' || a == 'i' || a == 'o' || a == 'u')
	    printf("%c is vowel",a);
	else
	    printf("%c is constant",a);
	
	
}